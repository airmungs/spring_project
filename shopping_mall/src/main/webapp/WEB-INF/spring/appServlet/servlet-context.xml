<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- @Controller 로 웹 애플리케이션 정의할 수 있게 해주는 태그 -->
    <mvc:annotation-driven />

    <!-- 정적 자원 핸들링 -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    <mvc:resources mapping="/upload/**" location="/upload/" />

    <!-- MVC에서 views의 jsp를 출력 -->
    <beans:bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>
	
	<!-- @이 붙은 클래스를 bean으로 자동 등록 -->
    <context:component-scan base-package="shopping_admin,pay,shopping_client" />
    
    <!-- 인터셉터 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <mvc:exclude-mapping path="/" />
            <mvc:exclude-mapping path="/login" />
            <mvc:exclude-mapping path="/add_master.do" />
            <mvc:exclude-mapping path="/add_masterok" />
            <mvc:exclude-mapping path="/idcheck" />
			<mvc:exclude-mapping path="/sailmall/**" />
            <mvc:exclude-mapping path="/resources/**" />
            <beans:bean class="shopping_admin.interceptor.AdminLoginInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>
</beans:beans>
